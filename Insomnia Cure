#include <bits/stdc++.h>

using namespace std;

int main()
{
    int k;//punched naga
    int l;//tail shut naga
    int m;//paws trampled naga
    int n;//threatened naga
    int o;//total naga
    int counter=0;

    cin>>k;
    cin>>l;
    cin>>m;
    cin>>n;
    cin>>o;
    //kalau ada yang 1 berarti tandanya otomatis kehitung semua
    if(k==1 || l==1 || m==1 || n==1)
    {
        o==counter;
    }
    //kalau tidak ada 1
    for (int i=1; i<=o; i++)
    {
        if(i%k==0 || i%l==0 || i%m==0 || i%n==0)
        {
            counter++;
        }
    }
    cout<<counter;
    return 0;
}
